"""Stable contracts consumed by the Fleet Control Center.

The control plane imports only this package.  Implementations may move inside
``weex_cli`` without changing the boundary exposed to Fleet.
"""
