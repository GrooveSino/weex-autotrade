<!-- Generated by skills/weex-api-development/scripts/sync_docs.py; do not edit. -->

> Source: [https://www.weex.com/api-doc/zh-CN/contract/Transaction_API/ModifyTpSlOrder](https://www.weex.com/api-doc/zh-CN/contract/Transaction_API/ModifyTpSlOrder)

> Synced: 2026-07-16T16:03:53+00:00

版本：V3(BETA)

# 修改止盈止损条件单 (TRADE)

- **POST** `/capi/v3/modifyTpSlOrder`

**请求权重**

10s order rate limit(X-ORDER-COUNT-10S)为1; 1min order rate limit(X-ORDER-COUNT-1M)为1; IP rate limit(X-USED-WEIGHT-1M)为0

**请求参数**

| 参数名 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| orderId | Long | 是 | 条件单 ID。 |
| triggerPrice | String | 是 | 新触发价，必须大于 0。 |
| executePrice | String | 条件必填 | 新执行价，设为 0 或省略表示改为市价执行。带单类型 API key 只支持市价平仓，必须传 0 或省略。 |
| triggerPriceType | String | 否 | 触发价类型：`CONTRACT_PRICE` 或 `MARK_PRICE`，默认 `CONTRACT_PRICE`。 |

**请求示例**

```
curl -X POST "https://api-contract.weex.com/capi/v3/modifyTpSlOrder" \
   -H "ACCESS-KEY:*******" \
   -H "ACCESS-SIGN:*******" \
   -H "ACCESS-PASSPHRASE:*****" \
   -H "ACCESS-TIMESTAMP:1659076670000" \
   -H "Content-Type: application/json" \
   -d '{
     "orderId": 812345678901234900,
     "triggerPrice": "71000",
     "executePrice": "71000",
     "triggerPriceType": "MARK_PRICE"
   }'
```

**返回参数**

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| success | Boolean | 是否修改成功。 |

**返回示例**

```
{
  "success": true
}
```
